name: Nextjs Workshop Demo

on:
  push:
    branches:
      - 'main'

jobs:
  lint-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          cache: 'npm'
      - run: node -v
      - run: npm ci
      - run: npm run lint
